# model-dash

A Streamlit dashboard to explore how model-building choices affect metrics across multiple datasets. Users select a dataset, choose preprocessing and model options, and compare results from precomputed experiments (no training inside the UI).

